namespaces:
  dns.googleapis.com/query:
    namespace: dns.googleapis.com/query
    metrics:
      "latencies":
        name: "latencies"
        type: dns_query
        unit: "Milliseconds"
        dataConvertor: "{data}/60"
        datatype: DISTRIBUTION
        regionFetcher: location
        samplingRate: 60
        dataDelay: 120
        description: "The distribution of latencies experienced during Google's handling
          of DNS requests."
      "response_count":
        name: "response_count"
        type: dns_query
        unit: "Per Request"
        dataConvertor: "{data}/60"
        datatype: INT64
        regionFetcher: location
        samplingRate: 60
        dataDelay: 120
        description: "The rate at which DNS responses are generated by a private DNS
          server."
